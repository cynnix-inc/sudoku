name: Lighthouse CI (removed)

on:
  workflow_call:
    inputs: {}
    secrets: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lighthouse CI workflow removed; Netlify Lighthouse plugin handles audits."


