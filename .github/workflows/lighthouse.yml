name: Lighthouse CI (noop)

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:
  workflow_call:
    inputs: {}
    secrets: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lighthouse audits handled by Netlify plugin; scheduled noop to preserve visibility."


