#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Ensure changelog is up-to-date before push
npm run --silent changelog
if ! git diff --quiet -- CHANGELOG.md; then
  echo "Updating CHANGELOG.md before push..."
  git add CHANGELOG.md
  git commit -m "docs(changelog): update local changelog"
fi



